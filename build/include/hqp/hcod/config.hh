/* 
 * This file has been automatically generated by the jrl-cmakemodules.
 * Please see https://github.com/jrl-umi3218/jrl-cmakemodules/blob/master/config.hh.cmake for details.
*/

#ifndef HQP_HCOD_CONFIG_HH
# define HQP_HCOD_CONFIG_HH

// Package version (header).
# define HQP_HCOD_VERSION_UNKNOWN_TAG 0 // Used to mention that the current version is unknown.
# define HQP_HCOD_VERSION "0.0.0"
# define HQP_HCOD_MAJOR_VERSION 0
# define HQP_HCOD_MINOR_VERSION 0
# define HQP_HCOD_PATCH_VERSION 0

#define HQP_HCOD_VERSION_AT_LEAST(major, minor, patch) (HQP_HCOD_MAJOR_VERSION>major || (HQP_HCOD_MAJOR_VERSION>=major && \
                                                             (HQP_HCOD_MINOR_VERSION>minor || (HQP_HCOD_MINOR_VERSION>=minor && \
                                                                                                     HQP_HCOD_PATCH_VERSION>=patch))))

#define HQP_HCOD_VERSION_AT_MOST(major, minor, patch) (HQP_HCOD_MAJOR_VERSION<major || (HQP_HCOD_MAJOR_VERSION<=major && \
                                                            (HQP_HCOD_MINOR_VERSION<minor || (HQP_HCOD_MINOR_VERSION<=minor && \
                                                                                                     HQP_HCOD_PATCH_VERSION<=patch))))

// Handle portable symbol export.
// Defining manually which symbol should be exported is required
// under Windows whether MinGW or MSVC is used.
//
// The headers then have to be able to work in two different modes:
// - dllexport when one is building the library,
// - dllimport for clients using the library.
//
// On Linux, set the visibility accordingly. If C++ symbol visibility
// is handled by the compiler, see: http://gcc.gnu.org/wiki/Visibility
# if defined _WIN32 || defined __CYGWIN__
// On Microsoft Windows, use dllimport and dllexport to tag symbols.
#  define HQP_HCOD_DLLIMPORT __declspec(dllimport)
#  define HQP_HCOD_DLLEXPORT __declspec(dllexport)
#  define HQP_HCOD_DLLLOCAL
# else
// On Linux, for GCC >= 4, tag symbols using GCC extension.
#  if __GNUC__ >= 4
#   define HQP_HCOD_DLLIMPORT __attribute__ ((visibility("default")))
#   define HQP_HCOD_DLLEXPORT __attribute__ ((visibility("default")))
#   define HQP_HCOD_DLLLOCAL  __attribute__ ((visibility("hidden")))
#  else
// Otherwise (GCC < 4 or another compiler is used), export everything.
#   define HQP_HCOD_DLLIMPORT
#   define HQP_HCOD_DLLEXPORT
#   define HQP_HCOD_DLLLOCAL
#  endif // __GNUC__ >= 4
# endif // defined _WIN32 || defined __CYGWIN__

# ifdef HQP_HCOD_STATIC
// If one is using the library statically, get rid of
// extra information.
#  define HQP_HCOD_DLLAPI
#  define HQP_HCOD_LOCAL
# else
// Depending on whether one is building or using the
// library define DLLAPI to import or export.
#  ifdef hqp_hcod_EXPORTS
#   define HQP_HCOD_DLLAPI HQP_HCOD_DLLEXPORT
#  else
#   define HQP_HCOD_DLLAPI HQP_HCOD_DLLIMPORT
#  endif // HQP_HCOD_EXPORTS
#  define HQP_HCOD_LOCAL HQP_HCOD_DLLLOCAL
# endif // HQP_HCOD_STATIC
#endif //! HQP_HCOD_CONFIG_HH
